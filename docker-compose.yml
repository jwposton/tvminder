services:
  tvminder:
    build: .
    image: ghcr.io/jwposton/tvminder:local
    ports:
      - "3000:3000"
    env_file:
      - .env
    user: "1000:1000"
    environment:
      DATABASE_URL: file:/data/tvminder.db
    volumes:
      - ./data:/data
    restart: unless-stopped
