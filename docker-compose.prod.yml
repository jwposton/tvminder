services:
  tvminder:
    image: ghcr.io/jwposton/tvminder:latest
    ports:
      - "3000:3000"
    env_file:
      - .env
    user: "1000:1000"
    environment:
      DATABASE_URL: file:/data/tvminder.db
      TZ: America/New_York
    volumes:
      - ./data:/data
    restart: unless-stopped
